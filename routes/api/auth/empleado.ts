import { HandlerContext, Handlers } from "$fresh/server.ts";
import { UserLogin } from "@/model/user.ts";

export const handler: Handlers = {
//   async POST(_req: Request, ctx: HandlerContext) {
//     const body = (await _req.json()) as UserLogin;

//   },
};